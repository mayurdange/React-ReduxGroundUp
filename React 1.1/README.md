using npm managed packages .. refer to Redux 1.2 for more info.

dependencies installed using:
npm install react --save
npm install react-dom --save


to run the app run use the commands: 
npm install
npm start