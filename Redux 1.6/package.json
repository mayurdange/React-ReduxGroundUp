{
  "name": "redux1.6",
  "version": "1.0.0",
  "description": "so far, we have included the scripts from different URL's\r in a real world application, this is unreliable as \r 1) our server availability depends on the server with those scripts.\r 2) we are using bandwidth of those servers (stealing bandwidth :( )",
  "scripts": {
    "start": "start Component.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redux": "^3.7.2"
  }
}
